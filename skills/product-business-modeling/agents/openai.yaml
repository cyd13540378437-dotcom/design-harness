interface:
  display_name: "Product Business Modeling"
  short_description: "业务模型建模、校验、单一用户总览与下游视图编译"
  default_prompt: "使用 $product-business-modeling 处理当前业务模型任务。先绑定 BM Work Item，使用标准文件和 Core State Schema，完成必要的人类决策、业务模型更新与一致性校验，再编译 docs/product/BUSINESS_MODEL_OVERVIEW.md，并只把该总览作为用户默认阅读入口。"

policy:
  allow_implicit_invocation: true
