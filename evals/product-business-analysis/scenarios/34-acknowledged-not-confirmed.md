# acknowledged 与 referential confirmation 的边界

## Given / 已知
系统给出 provisional / conditional 建议。

## When / 当
用户只回复“好的，我知道了”或单纯复述系统建议。

## Then / 则
只能记为 `acknowledged` 或保持 `pending`。

## When / 当
用户明确说“就按这个”“其他的都没问题”，且指向唯一推荐。

## Then / 则
可以记为 `confirmed`；若同轮还有追问，追问另行分类。
