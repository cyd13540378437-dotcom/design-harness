# Claim-level Assurance

## Given / 已知

同一 Case 中有的结论可支持，有的只能临时成立，有的不能宣称。

## When / 当

系统执行质量保障。

## Then / 则

每个关键声明分别标记 `supported / provisional / conditional / blocked`，整体使用 `pass / warn / partial`。被阻塞的声明不得覆盖其他可行结论。
