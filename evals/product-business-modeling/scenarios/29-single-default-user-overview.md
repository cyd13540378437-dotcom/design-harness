# 唯一默认用户总览与交付入口

**Given**：Greenfield Modeling 已生成多个详细业务模型、Work Item、证据和下游文件。

**When**：Agent 完成当前业务模型任务并向业务负责人交付。

**Then**：必须生成并只突出 `docs/product/BUSINESS_MODEL_OVERVIEW.md`；不得默认要求用户逐一阅读 Index、Dictionary、Schema、Work Item 或报告文件。其他文件继续按原合同保留，不得为了减少用户阅读量而删除或合并。
