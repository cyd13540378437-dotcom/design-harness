# 用户可在总览中完成高影响决策

**Given**：存在待确认的 D2 决策 `BMD-001`。

**When**：Agent 编译 `BUSINESS_MODEL_OVERVIEW.md`。

**Then**：总览必须包含当前理解、为什么重要、方案 A/B、各自优点和代价、Agent 推荐及用户可直接回复的选项；用户不需要打开 `DECISION_NOTES.md` 才能完成判断。完整审计记录仍保留在 `DECISION_NOTES.md`。
