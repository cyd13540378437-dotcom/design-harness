# Business Analysis Decision Support Loop v0.1.4

```text
恢复上下文
→ 解析混合轮次
→ 确认问题与锁定决策范围
→ 建立 Choice Set 与推荐
→ 证据 / 模型产生 Choice Delta
→ 用户决定
→ confirmed 时关闭 Choice Loop
→ 原决策层概念澄清（如有）
→ completion-review
→ 新证据或取舍变化时正式重开
```

该循环不要求每轮完整执行。Choice Loop 关闭后，除 review 外不得重新运行分析流水线。
