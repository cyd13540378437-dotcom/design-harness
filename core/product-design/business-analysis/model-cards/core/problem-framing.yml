schema_version: 1
model_id: problem-framing
title: 问题界定
category: core-decision
status: ready
purpose:
- 把用户原始输入转成可讨论、可确认、可选择的当前问题。
- 防止后续分析建立在误解或过度收窄的问题上。
use_when:
- 用户输入含糊、范围大或包含多个可能问题。
- 用户表达了一个方案，但背后问题不清楚。
- 高影响问题需要先确认理解。
avoid_when:
- 用户已经明确确认当前问题且没有歧义。
- 只是保存用户原始输入，不进入分析。
decision_questions:
- 用户真正想处理的选择题是什么？
- 当前问题是否被过度收窄？
- 哪些事项暂时不判断？
required_inputs:
- 用户第一次输入原文
- 上下文线索
- 用户是否表达了目标或担忧
minimum_viable_inputs:
- 用户第一次输入原文
outputs:
- Input Understanding Snapshot
- 潜在选择题
- 关键歧义
- 需要确认的理解
- Choice Delta
- 对当前业务选择的 Choice Delta
case_fields_updated:
- Decision Basis / 原始输入
- Decision Core / 当前决策问题
- Decision Basis / Why Now
- Decision Basis / 决策范围
- Decision Basis / 关键线索
interaction_prompts:
- 我先确认一下我的理解：……这个问题背后可能有 A/B/C 几种选择，你看哪个更接近？
- 你现在更想判断“是否做 X”，还是先判断“为什么出现 Y”？
usage_prompts:
  decision_relevance_prompt: 在使用《问题界定》前，先恢复当前 Decision Anchor。明确本模型试图降低哪个关键不确定性，以及不同分析结果会让哪个业务选项更强、更弱、被排除或变成有条件成立。如果结果不会改变选项排序、风险、阈值、投入方式或是否继续，则停止使用本模型。分析结束后必须输出中文
    Choice Delta。 在执行前必须明确本模型会改变 Choice Set、选项后果、当前推荐或推荐置信度中的哪一项；若不会，停止使用。 若 choice_loop_status=closed，除正式 review_signal 外必须停止使用本模型；不得越过
    Decision Scope Lock；不得生成实施步骤、正式 Schema、公式权重、样例数据、代码或执行提议。
  routing_prompt: 仅作内部适配判断。判断当前输入是否需要问题界定。若用户原文存在多个可能解释、多个潜在选择题、或系统准备做高影响分析，则必须使用本模型。 若 choice_loop_status=closed，除正式 review_signal
    外必须停止使用本模型；不得越过 Decision Scope Lock；不得生成实施步骤、正式 Schema、公式权重、样例数据、代码或执行提议。
  user_input_prompt: '仅当缺少会改变当前业务选项排序或风险判断的关键输入时使用；一次最多用简体中文提出 1–2 个问题。只有当缺失信息会改变业务选项排序、风险判断、成功阈值或是否现在决策时，才向用户提问。不要为了填满模型字段而提问。

    请用用户语言反馈理解，不要先上专业术语。优先输出 2–4 个可能选择题，让用户确认哪个最接近。'
  execution_prompt: 只使用当前 Case 已接纳的事实、假设和证据执行分析；不得扩大问题边界或进入无关实施设计。基于原始输入，提取表层问题、潜在选择题、关键歧义、暂不判断事项。不要判断新旧 Case，也不要进入模型分析。 若
    choice_loop_status=closed，除正式 review_signal 外必须停止使用本模型；不得越过 Decision Scope Lock；不得生成实施步骤、正式 Schema、公式权重、样例数据、代码或执行提议。
  analysis_run_prompt: 用简体中文写入 Analysis Run，必须包含 decision_relevance、choice_delta、limitations 和 confidence。将结果写入 Input Understanding
    Snapshot，并同步更新 Decision Case 的原始输入、当前问题、关键线索。 输出必须包含 Choice Delta。 作为 internal_control，quick / standard 默认直接更新 Decision
    Case 或中央综合，不强制创建独立 Analysis Run；仅 deep / audit 或确有独立审计价值时落盘。
  conclusion_prompt: 本 Prompt 只向中央决策综合器返回候选含义，不得直接向用户输出最终建议或结束对话。本提示只供统一决策综合器内部使用，不得直接向用户输出最终结论。如果理解仍不清楚，提供中文理解确认；如果足够清楚，只输出已确认商业问题、候选解法与暂不判断事项，不进入方案推荐。
    向中央综合器返回受影响选项、推荐候选、接受的代价、证据边界和改变推荐的条件。 若 choice_loop_status=closed，除正式 review_signal 外必须停止使用本模型；不得越过 Decision Scope Lock；不得生成实施步骤、正式
    Schema、公式权重、样例数据、代码或执行提议。
  misuse_guard_prompt: 检查问题漂移、解法前提未挑战、无 Choice Delta、伪精确和越界进入产品/技术设计。检查是否过早路由、过早套模型、过度改写用户原意、丢失原始输入。 检查是否用 Assurance 替代 Recommendation，或因证据不足只要求用户补材料而没有给出当前现实路径。
    若 choice_loop_status=closed，除正式 review_signal 外必须停止使用本模型；不得越过 Decision Scope Lock；不得生成实施步骤、正式 Schema、公式权重、样例数据、代码或执行提议。
common_misuses:
- 把用户方案当成真实问题
- 省略原始输入
- 第一步就判断新旧 Case
- 用专业术语压倒用户
quality_checks:
- 是否完整保留原始输入
- 是否至少列出一个潜在选择题
- 是否说明暂不判断事项
- 高影响歧义是否请求确认
- 是否说明本模型对当前业务选择产生了什么 Choice Delta
- 低相关结果是否被排除出用户可见结论
- 是否保持当前 Decision Anchor
- 是否产生可说明的 Choice Delta
- 是否以中文写入 Analysis Run
- 是否仅向中央综合器提供结论素材
- 是否形成或更新了真实 Choice Set
- 是否产生可说明的 Choice Delta
- 是否向中央综合器提供推荐候选与改变推荐的条件
- Choice Loop 关闭后是否停止运行模型
- 是否遵守 Decision Scope Lock
- 是否避免实施产物与执行提议
combines_well_with:
- decision-routing
- evidence-classification
- option-assessment
example_analysis_run:
- 用户问“要不要做企业版？” → 界定为可能涉及目标客户、产品能力、销售交付和定价模式的选择题。
output_language: zh-CN
runtime_role: internal_control
