# Runtime Overlays

These overlays are not part of the neutral project template. The Runtime Adapter Resolver selects one overlay before materialization.

Use:

```text
codex/       for Codex
claude-code/ for Claude Code
cursor/      for Cursor
```

Do not apply every overlay by default. Apply multiple overlays only when the user explicitly asks for multi-runtime support.
