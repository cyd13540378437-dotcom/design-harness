# Reference Images

Place project-provided reference images for idea-storm-lab in this directory.

This example uses the built-in `warm-creator-tool` and `editorial-content-system` reference packs instead of external screenshots. Reference images should be treated as visual evidence to analyze, not assets to copy.
