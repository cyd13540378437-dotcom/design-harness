# Engineering Context

## Current Baseline

- Upgrade UI must use existing modal and settings page primitives.
- Plan capability checks should be provided by product data, not hardcoded in copy-only UI.

